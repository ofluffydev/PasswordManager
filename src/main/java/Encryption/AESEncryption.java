package Encryption;

public class AESEncryption {
    public static String encrypt(String password, String key) {
        // Place holder
        return "Place holder";
    }
    // Now, we could just use the JCA to encrypt the password, but that would be boring

    // Let's just do the whole thing ourselves! Fun!


}
